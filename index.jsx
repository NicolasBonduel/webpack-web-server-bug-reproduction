/* eslint-disable no-undef */
import React from 'react';
import ReactDOM from 'react-dom';

const init = (selector) => {
  ReactDOM.render(<div>HELLO WORLD</div>, selector);
};

// eslint-disable-next-line import/prefer-default-export
export { init };
